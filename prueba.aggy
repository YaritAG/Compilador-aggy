int nota = 85;
int grado = 0;

if (nota >= 90) {
    grado = 5;
} elseif (nota >= 80) {
    grado = 4;
} elseif (nota >= 70) {
    grado = 3;
} elseif (nota >= 60) {
    grado = 2;
} else {
    grado = 1;
}

print(grado);
